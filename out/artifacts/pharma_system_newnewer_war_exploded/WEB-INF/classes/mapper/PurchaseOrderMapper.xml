<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pharma.mapper.PurchaseOrderMapper">
    <resultMap id="BaseResultMap" type="com.pharma.model.PurchaseOrder">
        <id column="purchase_id" property="purchaseId"/>
        <result column="purchase_code" property="purchaseCode"/>
        <result column="supplier_id" property="supplierId"/>
        <result column="employee_id" property="employeeId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        purchase_id, purchase_code, supplier_id, employee_id, total_amount, status, created_at, updated_at
    </sql>

    <select id="selectByPageAndKeyword" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM purchase_order
        <where>
            <if test="keyword != null and keyword != ''">
                AND (purchase_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByKeyword" resultType="int">
        SELECT COUNT(*)
        FROM purchase_order
        <where>
            <if test="keyword != null and keyword != ''">
                AND (purchase_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM purchase_order
        WHERE purchase_id = #{id}
    </select>

    <insert id="insert" parameterType="com.pharma.model.PurchaseOrder" useGeneratedKeys="true" keyProperty="purchaseId">
        INSERT INTO purchase_order (
            purchase_code, supplier_id, employee_id, total_amount, status, created_at, updated_at
        )
        VALUES (
            #{purchaseCode}, #{supplierId}, #{employeeId}, #{totalAmount}, #{status}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update" parameterType="com.pharma.model.PurchaseOrder">
        UPDATE purchase_order
        SET
            purchase_code = #{purchaseCode},
            supplier_id = #{supplierId},
            employee_id = #{employeeId},
            total_amount = #{totalAmount},
            status = #{status},
            updated_at = #{updatedAt}
        WHERE purchase_id = #{purchaseId}
    </update>

    <delete id="deleteById">
        DELETE FROM purchase_order WHERE purchase_id = #{id}
    </delete>
</mapper> 